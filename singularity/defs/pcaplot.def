Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/pcaplot.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n pca_plotnine_env
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/pca_plotnine_env/bin:$PATH"

%runscript
    exec "$@"
