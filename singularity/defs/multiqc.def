Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/multiqc.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n multiqc
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/multiqc/bin:$PATH"

%runscript
    exec "$@"
