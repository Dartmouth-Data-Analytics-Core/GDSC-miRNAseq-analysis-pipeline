Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/bbmap.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n bbmap
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/bbmap/bin:$PATH"

%runscript
    exec "$@"
