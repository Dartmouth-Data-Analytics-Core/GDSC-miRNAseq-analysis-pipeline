Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/mirtop.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n mirtop
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/mirtop/bin:$PATH"

%runscript
    exec "$@"
