Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/seqcluster.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n seqcluster
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/seqcluster/bin:$PATH"

%runscript
    exec "$@"
