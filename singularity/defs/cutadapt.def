Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/cutadapt.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n cutadapt
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/cutadapt/bin:$PATH"

%runscript
    exec "$@"
