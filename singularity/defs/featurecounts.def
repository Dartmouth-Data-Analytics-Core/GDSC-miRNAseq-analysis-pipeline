Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/featurecounts.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n featurecounts
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/featurecounts/bin:$PATH"

%runscript
    exec "$@"
